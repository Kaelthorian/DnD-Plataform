# Spell Utils

This folder is reserved for pure helpers extracted from `../spell-data.js` when they have multiple real consumers. Keep the active normalization/profile API in `spell-data.js` until an extraction reduces coupling without creating a second contract.
